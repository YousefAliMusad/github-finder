import { BiSearchAlt } from "react-icons/bi"
// import "../style/header.css"
import { Link } from "react-router-dom"

function Header() {
    if (window.innerWidth <= 800) {
    }
    return(
    <div>
        {/* <header className="w-100 bg-dark mb-5 d-flex text-white" style={{ minHeight:"60px", justifyContent:'center', alignItems:'center'}}>
            <div className="eye-parent d-flex" style={{overflow:"hidden" , width:'160px', height:'50px', borderRadius:'50%', background:'white', justifyContent:'center', alignItems:'center'}}>
                <div style={{width:'35px' ,height:'35px', background:'black', borderRadius:'50%' }}></div>
            </div>
        </header> */}
    </div>
    )
}

export default Header